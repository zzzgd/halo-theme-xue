{
    "apps" : [{
        "name"      : "hello Blog",
        "script"    : "server/app.js",
        "env": {
            "COMMON_VARIABLE": "true"
        },
        "env_production" : {
            "NODE_ENV": "production"
        }
    }],
    "deploy" : {
            "production" : {
                "user" : "root",
                "host" : "139.9.241.164",
                "ref"  : "origin/master",
                "repo" : "https://github.com/xzzai/halo-xue-react-next",
                "path" : "/opt/app/react-next-xue",
                "pre-setup" : "rm -rf /opt/app/react-next-xue/source",
                "post-deploy" : "cnpm install && npm run build && pm2 startOrRestart ecosystem.json --env production",
                "ssh_options": ["StrictHostKeyChecking=no", "PasswordAuthentication=yes"],
                "env"  : {
                    "NODE_ENV": "production"
            }
        }
    }
}
